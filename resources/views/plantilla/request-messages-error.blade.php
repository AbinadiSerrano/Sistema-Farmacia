<script>
    // Espera a que el documento esté completamente cargado
    document.addEventListener('DOMContentLoaded', function() {
        // Selecciona el mensaje de error
        var errorNombrec = document.getElementById('nombrec');
        var errorNombrep = document.getElementById('nombrep');
        var errorCatgeoriap = document.getElementById('categoriap');
        var errorNombrel = document.getElementById('nombrel');
        var errorDireccionl = document.getElementById('direccionl');
        var errorCorreol = document.getElementById('correol');
        var errorTelefonol = document.getElementById('telefonol');
        var errorPaisl = document.getElementById('paisl');
        var errorCiudadl = document.getElementById('ciudadl');
        var errorNombrea = document.getElementById('nombrea');
        var errorUbicaciona = document.getElementById('ubicaciona');
        var errorNombrem = document.getElementById('nombrem');
        var errorPreciom = document.getElementById('preciom');
        var errorFecha_vencimientom = document.getElementById('fecha_vencimientom');
        var errorIndicacionesm = document.getElementById('indicacionesm');
        var errorLaboratorio_idm = document.getElementById('laboratorio_idm');
        var errorPresentacion_idm = document.getElementById('presentacion_idm');
        // CATEGORIA
        if (errorNombrec) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorNombrec.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        //PRESENTACION
        if (errorNombrep) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorNombrep.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorCatgeoriap) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorCatgeoriap.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

            //LABORATTORIO
        if (errorNombrel) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorNombrel.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorDireccionl) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorDireccionl.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

        if (errorCorreol) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorCorreol.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorTelefonol) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorTelefonol.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorPaisl) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorPaisl.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

        if (errorCiudadl) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorCiudadl.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
            //ALMACEN

            if (errorNombrea) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorUbicaciona.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

        if (errorUbicaciona) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorUbicaciona.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        ///MEDICAMENTOS
        if (errorNombrem) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorNombrem.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorPreciom) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorPreciom.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
        if (errorFecha_vencimientom) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorFecha_vencimientom.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

        if (errorIndicacionesm) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorIndicacionesm.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }
            //ALMACEN

            if (errorLaboratorio_idm) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorLaboratorio_idm.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }

        if (errorPresentacion_idm) {
            // Establece un temporizador de 5 segundos
            setTimeout(function() {
                // Oculta el mensaje de error
                errorPresentacion_idm.style.display = 'none';
            }, 5000); // 5000 milisegundos = 5 segundos
        }



    });
</script>